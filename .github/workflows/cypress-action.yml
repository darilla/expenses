name: Cypress tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-16.04
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - run: npm install -g bats
      - run: bats -v
      - name: Cypress run
        uses: cypress-io/github-action@v2
